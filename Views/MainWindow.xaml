<Window x:Class="Launcher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Launcher.Converters"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Launcher" Height="700" Width="1100" WindowStartupLocation="CenterScreen"
        Loaded="MainWindow_OnLoaded">

    <Window.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Margin="10,10,5,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="16,16,16,10">
                    <TextBlock Text="Library" HorizontalAlignment="Center" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center"/>
                    <Button x:Name="RefreshGamesButton"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Обновить список игр"
                            HorizontalAlignment="Right"
                            Click="RefreshGamesButton_Click">
                        <materialDesign:PackIcon Kind="Refresh" />
                    </Button>
                </Grid>

                <ListBox Grid.Row="1" x:Name="GameListBox" DisplayMemberPath="Name" SelectionChanged="GameListBox_SelectionChanged"
                         Background="Transparent" BorderThickness="0" Foreground="{DynamicResource MaterialDesignBodyLight}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="5,0">

                    <ListBox.Resources>
                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource MaterialDesignScrollBarMinimal}"/>
                    </ListBox.Resources>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="Padding" Value="12,8"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="5,10,10,10">
            <materialDesign:Card x:Name="DetailsPanel" Padding="32" UniformCornerRadius="8" Visibility="Hidden">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.RowSpan="3" x:Name="PlayButton" Click="PlayButton_Click" 
                            Style="{StaticResource MaterialDesignFloatingActionDarkButton}" 
                            ToolTip="Play!" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="25">
                        <materialDesign:PackIcon Kind="Play" Height="32" Width="32" />
                    </Button>

                    <TextBlock x:Name="GameTitleTextBlock" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,25" TextWrapping="Wrap"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Height="280" Width="210" CornerRadius="8" Background="{DynamicResource MaterialDesignDivider}"
                                VerticalAlignment="Top" HorizontalAlignment="Left">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.4" BlurRadius="8"/>
                            </Border.Effect>
                            <Image x:Name="GamePosterImage" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="25,0,0,0">
                            <TextBlock Text="AppID (Steam)" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,0,2"/>
                            <TextBlock x:Name="AppIdTextBlock" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,15"/>

                            <TextBlock Text="Path" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,0,2"/>
                            <TextBlock x:Name="InstallPathTextBlock" Style="{StaticResource MaterialDesignBody1TextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card x:Name="PlaceholderPanel" Padding="32" UniformCornerRadius="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Select the game" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>